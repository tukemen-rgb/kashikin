# 引き継ぎ

貸金業務取扱主任者の過去問1000問データセットと学習アプリ。
これまでの経緯・決めたこと・決めた理由・未着手をまとめたものです。

- リポジトリ　`tukemen-rgb/kashikin`
- ブランチ　`claude/verify-qa-matching-s3jfhi`（pull request はまだ作っていません）
- アプリ　https://claude.ai/code/artifact/f7cab540-8471-4346-af48-5c52d403c455
- デザインシステム　https://claude.ai/code/artifact/aa240965-a7f1-4832-b49f-b368b30340b0

---

## 1. いちばん重要な制約

**問題文は一言一句そのまま。** 見せ方だけを変え、文字を足す・削る・置き換えることは一切しません。
ビルドのたびに全1000問（問題文・記述ａ〜ｄ・選択肢・注記の5,053項目）について
描画テキストと原文を突き合わせる自動検証を回しており、現在**不一致0件**です。

この制約がある理由は、最初にアップロードされた7ファイルの解説を検証したところ、
令和6年・令和7年の2ファイルを除いて**解説が設問と別の論点を説明していた**からです（`検証結果.md`）。
生成された文章は信用しない、というのがこのプロジェクトの前提になっています。

---

## 2. データ

| 項目 | 内容 |
|---|---|
| 収録 | 第1回（平成21年度）〜第20回（令和7年度） 各50問・**計1000問** |
| 出典 | 日本貸金業協会が公開している試験問題PDF・正答PDF |
| 分野 | 法令551／貸付実務290／資金需要者保護99／財務会計60 |
| 形式 | 単純択一827／組合せ101／個数72。否定形（「適切でないもの」）456 |

### 裏付け

1. **正答1000件** — 公式正答PDFと一致（1000/1000）
2. **本文5,053項目** — 別エンジン pypdfium2 で読み直して **100.00%一致**
3. **数字を含む本文2,196項目**（第10〜20回）— 過去問ドットコムと **99.86%一致**。
   食い違った3件のうち1件は他社側の誤記（「0円」を「O円」＝アルファベットのオー）
4. **第1〜9回の数字** — 埋め込みフォントの字形問題があるため、ページを画像に起こして目視確認
5. **KakomonAI との照合は見送り** — 選択肢3,340件のうち120件（3.6%）で本文が食い違い、
   確認したところ同サイト側の誤りだったため基準から外した

### 分類の検証

分野はPDFの見出しから取得。第5回以降の16回すべてが公式の出題構成 **27/15/5/3 と完全一致**。
第1〜4回だけ構成が違うのは国家資格化前の試験だからで、他社データとも一致しています。

4分野は粗すぎる（法令だけで551問）ので、その下に**47テーマ**を用意して全1000問を1つずつ割り当て、
未分類は1問だけです。条文名は出題者自身が本文で括弧書きしているものだけを採用（推測なし、77条文）。

---

## 3. アプリの機能

`app/index.html` を開くだけで動きます。サーバー不要・通信なし。記録はブラウザに保存。

| 機能 | 中身 |
|---|---|
| 出題エンジン | 忘却曲線に沿った復習期日（SM-2風）＋分野別正答率による弱点重み付け |
| 範囲 | 全1000問／分野4／テーマ47／回20 |
| 出題の狙い | おまかせ（弱点＋復習）／間違えた問題／未着手／頻出論点 |
| 模試 | 全20回それぞれ50問・2時間。**その回の公式合格基準点**で合否判定 |
| 弱点診断 | 引っかけの型・テーマ・出題形式・否定形・解答時間の5軸で集計 |
| 引っかけの型 | 12型を選択肢の対立から機械的に検出。1000問中440問に付与 |
| 似ている問題 | 文字3-gramのTF-IDFコサイン類似度で各問に最大5問を事前計算 |
| 合格可能性 | 分野別正答率から本番構成27/15/5/3の得点分布を正規近似 |
| 続ける仕掛け | 段位（十級〜五段）・連続日数・到達度マトリクス・実績12種 |

### AI機能について（重要）

**公開ページから大規模言語モデルは呼べません。** Artifact は厳格なCSPで外部ホストへの通信が遮断され、
この利用者に付与されている runtime capability は `downloads` と `mcp` だけです。

そのうえで、仮に呼べても解説の自動生成はしません（§1の理由）。
いま「AIがアドバイスする」と見える部分はすべて、**原文からの抽出と解答履歴の統計**です。
引っかけの型ごとの着眼点は固定文で、自分の正誤の集計と組み合わせて提示しているだけです。

自前でホストして Haiku 4.5 を呼ぶ案（1問あたり約0.45円）は提示しましたが、未着手のままです。

---

## 4. デザインの経緯

指摘を受けるたびに方向を変えているので、順番に残します。

| # | 指摘 | やったこと |
|---|---|---|
| 1 | 1000件インプットして※絶対 | 20回×50問を全件抽出 |
| 2 | 学習者がワクワクする | 段位・実績・連続日数・到達度 |
| 3 | 長文を見やすく（一言一句同じで） | 一文ずつ改行・定義かっこを薄く・着目語に下線 |
| 4 | 資格アプリのUIUXを500件取得 | 4,554本を収集（`research/apps.csv`） |
| 5 | 分析結果でUIUXと構造を修正 | ホームを「次にやる1つ」に集約 |
| 6 | 〇条って言われても分からない／20代に刺さる色に | 47テーマ＋条文名。明朝→ゴシック |
| 7 | フォントをメイリオに統一／AIが傾向を助言 | メイリオ1本。弱点診断5軸 |
| 8 | **今の画面がAI感が強い** | 同ジャンル実機と突き合わせて全面刷新（下記） |
| 9 | **3色から4色に** | 6色に散っていたのを役割で4色に畳んだ |
| 10 | **緑基調のアプリを300件** | 実測調査（下記）。主色を深緑へ |
| 11 | 初回画面のスクショ | ゼロ状態を作り込み |

### 8 でやったこと — AI感の除去

同ジャンルの実機（行政書士・社会福祉士・ケアマネ・SPI・数学検定・地理）の画面を並べて共通点を抽出。

| 実機で共通 | 反映 |
|---|---|
| ブランド色は1色 | 2色対等をやめた |
| ヘッダーは色ベタ塗り | 濃い色のヘッダーバー＋朱の下罫 |
| 角丸ほぼ0 | 14px → 4px |
| 影なし | `box-shadow` 全廃 |
| グラデーションなし | XPバー等を単色に |
| 一覧は詰める | テーマ47件が1画面に入る密度 |
| 強調は赤い下線 | 蛍光ペン風の塗りをやめた |
| 「次へ」は朱のボタン | 画面下に固定 |

AI感の正体は色より**作りの癖**でした。藍紫 `#4B5EE4` ＋珊瑚 `#FF6B35` ＋大きい角丸＋全面に影、
という組み合わせが生成物の定番になっているのが原因です。

### 10 でやったこと — 色の実測調査

4,554本の App Store 商品ページを取得し、**実際のスクショを1枚ずつ落として画素で色を測りました**
（縦横比1.4以上の画像だけをスクショと判定してアイコンを除外、白・黒・灰を除いた有彩色の最頻色相＝基調色）。
判定できたのは3,620本。

| 母集団 | 緑 | 青 |
|---|---:|---:|
| 全3,620本 | 11.2%（4位） | 41.6% |
| 資格・検定・試験 1,706本 | 11.4%（2位） | 45.5% |
| 「過去問」を名乗る 304本 | 16.1%（2位） | 33.9% |
| **うちレビュー300件以上 29本** | **34.5%（1位）** | **6.9%** |

絞るほど緑が上がり、上位では青が消えます。
首位グループ（独学TODAY／Trips LLC）は宅建 `#01843A`・ITパスポート `#279F00`・
行政書士 `#03843A`・公務員 `#01602F` と**全部深緑**で、しかも画面構造が本アプリとほぼ同じでした。

緑300本の相方の色は **141本（47%）が「なし」**（緑＋無彩色のみ）。足す場合は橙43／赤24。

成果物は `research/green-apps.csv`（300本）、`research/緑基調アプリ300.md`（測り方と集計）、
`green-leaders.jpg`／`green-screens.jpg`／`green-icons.jpg`／`green-swatches.png`。

---

## 5. いまの配色 — 4色だけ

| 色 | 意味 | 使う場所 |
|---|---|---|
| **深緑** `#0F7A3C` | 操作するもの | ヘッダー・ボタン・リンク・選択中 ／ **法令** |
| **朱** `#C6402A` | 先へ進む／誤り | 「次へ」「学習をはじめる」・不正解・着目語の下線・連続日数 ／ **貸付実務** |
| **紺** `#134E8C` | できた | 正解・段位・XP・実績・到達度 ／ **資金需要者保護** |
| **紫** `#6A4A9C` | あとで見直す | 復習の期日・付箋・頻出 ／ **財務会計** |

決め方のポイント：

- **4分野の色分けにこの4色をそのまま使っている。** 分野が4つある以上、色は最低4つ要る。
  分野色と機能色を別々に持つ必要がなかったので重ねた
- `--ng`（不正解）は朱、`--gold`（段位・実績）は紺の**別名**。実体は4色
- 主色が緑になったので「できた」は紺に移した。日本の採点は〇も✕も赤ペンなので、
  「正解＝緑」は文化的な前提ではない
- 到達度の「全問一発正解」だけは4色すべての上に乗るため、色ではなく地色の縁取りで示す
- 正答率の3段階は 紺（60%以上）／深緑（30%以上）／朱

文字は全画面メイリオ。ヘッダーは主色より一段濃い `#0A5A2C`。ライト／ダーク両対応。

---

## 6. 初回起動時の画面

履歴ゼロの状態が設計されておらず、何も解いていないのに朱色の0が3つ並び、
空の週バーが8本立っていました。直した内容：

- 0 のときの数字は淡色。値が付いたら朱に変わる
- 記録がない週バーは描かない
- 一度も解いていないときは数字と週バーを畳み、
  「解いた日にチェックが付きます。今日が1日目です。」の一行にする
- 今日の丸の縁を朱から深緑へ（初回に赤い輪が付くと失点に見える）
- 復習が0件のときのリングと見出しの数字を紫から深緑へ
- 今日の目標を達成したらリングを閉じて 0 ではなく ✓
- 段位の行を折り返し単位ごとに分け、「正答率 —」だけが取り残されないようにした

---

## 7. デザインシステム（`design/`）

トークンとコンポーネントを **12枚のカード**に切り出してあります。

| Foundations | Components |
|---|---|
| 配色 ／ 文字 ／ 形と密度 | ヘッダーバー ／ ボタン ／ タグ ／ 一覧の行 ／ 設問 ／ 記述カード ／ 弱点診断 ／ 進み具合 ／ 状態 |

- 各ファイルの1行目に `<!-- @dsCard group="…" name="…" subtitle="…" -->`。
  Claude Design はこの印からペイン一覧を組むので、別途の登録は不要
- **`:root` は `app/template.html` から機械的に写している**ので二重管理にならない。
  アプリの色を変えれば `tools/design/*.py` の再生成で追従する
- `design/index.html` を開けば、接続しなくても同じ単位で見られる

### Claude Design の状態 → **未接続**

`DesignSync` の認可には `/design-login` が必要で、それには対話端末が要ります。
Claude Code on the web はブラウザ実行なので通りません。繋ぐ経路は3つ：

| | やり方 | 備考 |
|---|---|---|
| **A** | claude.ai/design でプロジェクトを作り、**GitHub から `design/` を取り込む** | いちばん早い。`@dsCard` 済みなので追加作業なし |
| **B** | Claude Design 側の **「Handoff to Claude Code」→ Web** | ワークスペースにプロジェクトが入るので、そのまま `DesignSync` で push できる |
| **C** | **Claude Design の MCP サーバー**を接続 | 継続的に使うならこれ。認可で止まらなくなる |

繋がったあとの順序は `list_projects` → `create_project` → `finalize_plan`（書き込むパスを固定）
→ `write_files`。詳細は `design/README.md`。

Claude Design 自体は claude.ai/design、Pro / Max / Team / Enterprise 向け（Free 不可、
Enterprise は既定オフ）。利用枠はチャットや Claude Code と共有です。

---

## 8. ファイルの場所

```
data/questions.json        全1000問
data/questions.csv         同上（BOM付きUTF-8・引っかけの型の列つき）
data/themes.json/.csv      47テーマ
data/traps.json            引っかけ12型

app/template.html          アプリの雛形（ここを編集する）
app/index.html             ビルド済み（約2MB）

design/index.html          デザインシステムのビューア
design/foundations/*.html  配色・文字・形
design/components/*.html   コンポーネント9枚
design/README.md           Claude Design への push 手順

research/apps.csv          資格アプリ4,554本
research/green-apps.csv    緑基調アプリ300本
research/緑基調アプリ300.md  色の実測調査
research/UIUX調査.md        最初のUI/UX調査
research/sheet-1〜3.jpg     画面一覧

tools/parse.py             PDF → db_raw.json
tools/build.py             図表の手当て・分類付与 → db.json
tools/enrich.py            条文抽出・頻出度・似ている問題
tools/themes.py            47テーマの割り当て・条文名の抽出
tools/traps.py             引っかけ12型の検出
tools/verify_engine.py     pypdfium2 との照合
tools/verify_thirdparty.py 他社データとの照合
tools/ux/                  色調査（取得・判定）
tools/design/              デザインシステムの生成

検証結果.md                 最初の7ファイルの検証結果
```

### ビルドと検証

アプリの再ビルドは `app/template.html` の `/*__DATA__*/` `/*__META__*/` `/*__TOPICS__*/`
`/*__TRAPS__*/` にJSONを差し込むだけです（`mkapp.py`）。

Playwright の回帰テストが7本あります。**`t7` が一言一句一致の検証**で、これは必ず0件でなければなりません。
残りは模試（t8）・学習の流れ（t9）・履歴あり（t10）・モバイル（t11）・テーマとダーク（t12）・診断（t13）。
Chromium は `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`。

---

## 9. 未着手・判断待ち

- **Claude Design への push** — §7 の A / B / C のどれかで接続してもらう必要あり
- **解説** — 載せていません。§1 の理由。自前ホストで Haiku 4.5 を呼ぶ案（約0.45円/問）は保留
- **テーマ一覧の正答率の色** — 良い＝紺、悪い＝朱。主色が緑になったので、
  紺が「良い」に見えるかは実使用で確かめたい（違和感があれば要調整）
- **pull request** — 明示の依頼がないので作っていません

## 10. 運用上の約束

- 開発と push は `claude/verify-qa-matching-s3jfhi` のみ
- pull request は明示的に頼まれたときだけ作る
- コミットメッセージ・PR・コードコメントにモデル識別子を書かない
